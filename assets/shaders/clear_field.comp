#version 450 core
layout(local_size_x = 256, local_size_y = 1, local_size_z = 1) in;

layout(std430, binding = 6) buffer FieldBits {
    uint fieldBits[];
};

uniform uint fieldSize;

void main() {
    uint idx = gl_GlobalInvocationID.x;
    if (idx >= fieldSize) return;
    fieldBits[idx] = 0u;
}
